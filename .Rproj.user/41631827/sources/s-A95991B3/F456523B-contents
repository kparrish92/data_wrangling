library(tidyverse)
library(untidydata)



mtcars %>% 
  group_by(., cyl) %>% 
  summarise(., mean_mpg = mean(mpg))



## TidyR 

glimpse(pre_post)

pre_post = pre_post

?separate

#separate 
pre_post %>% 
  separate(col = "spec", into = c("group", "level"), sep = "_")

#unite 